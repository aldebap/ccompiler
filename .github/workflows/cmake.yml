name: Build all with CMake

on: [push]

env:
  SOURCE_PATH: ${{ github.workspace }}/src/c/main
  TEST_PATH: ${{ github.workspace }}/src/c/test
  BUILD_PATH: ${{ github.workspace }}/build
  BUILD_TYPE: Debug

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: mkdir
      shell: bash
      working-directory: ${{ github.workspace }}
      run: mkdir $BUILD_PATH
      
    - name: Create Build Environment
      shell: bash
      working-directory: $BUILD_PATH
      run: cmake -E make_directory $BUILD_PATH

    - name: Configure CMake
      shell: bash
      working-directory: $BUILD_PATH
      run: make cmake -DCMAKE_BUILD_TYPE=$BUILD_TYPE

    - name: Build
      shell: bash
      working-directory: $BUILD_PATH
      run: cmake -DSOURCE_DIRECTORY=$SOURCE_PATH -DTEST_DIRECTORY=$TEST_PATH --build . --config $BUILD_TYPE

    #- name: Test
    #  working-directory: $BUILD_PATH
    #  shell: bash
      # Execute tests defined by the CMake configuration.  
      # See https://cmake.org/cmake/help/latest/manual/ctest.1.html for more detail
    #  run: ctest -C $BUILD_TYPE
